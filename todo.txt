    { Fix }
* make line counting in TokenEmitter more robust
* maybe matcher/pattern in skipUntil in tikenizer can be optimized

    { Consider }
*

    { Implement }
* recursive toString for vectors
* mechanism to "promote" exception, so the higher levels can append details
* context error section must contain function name
